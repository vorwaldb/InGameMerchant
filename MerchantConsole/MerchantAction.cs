namespace MerchantConsole
{
    /// <summary>
    /// Enum defining the different actions that can be performed with a merchant
    /// </summary>
    public enum MerchantAction
    {
        Buy = 1,
        Sell = 2,
        Exit = 3
    }
}